# Cedar – Development Report
## 2026-08-15

### 1. Database & Docker

PostgreSQL is running through Docker Compose using:

- Database: cedar
- User: cedar
- PostgreSQL container: cedar-db
- Host port: 5432

The database connection was verified successfully from the backend virtual environment.

### 2. Backend environment

The backend virtual environment is configured and psycopg2 is installed and working.

Database connectivity was verified with:

DATABASE OK

The backend uses the PostgreSQL connection through DATABASE_URL.

### 3. Alembic migration

A new migration was generated successfully:

913038ade754_add_shifts_and_employee_shifts.py

The migration was generated with Alembic autogenerate and detected:

- new shifts table
- new employee_shifts table
- index on shifts.code
- index on shifts.organization_id
- index on employee_shifts.employee_id
- index on employee_shifts.shift_id
- index on employee_shifts.work_date

### 4. Workforce / Shift functionality

Added or updated backend components for:

- Shift model
- EmployeeShift model
- workforce calculations
- demand calculations
- related schemas and routers
- backend services

### 5. Docker Compose

The Docker environment was restarted successfully.

Running services:

- cedar-db
- cedar-backend
- cedar-frontend

PostgreSQL is exposed on port 5432.
Backend is exposed on port 8000.
Frontend is exposed on port 5173.

### 6. Important troubleshooting

The initial database authentication problem was caused by the local connection configuration not matching the Docker PostgreSQL endpoint.

The working local backend connection was verified using:

127.0.0.1:5433

while Docker maps the PostgreSQL container port 5432 to host port 5433.

The final connection test returned:

DATABASE OK

### 7. Git

The changes are ready to be committed to the main branch.

The .env and .venv files are not intended to be committed.

---

## END OF DAY — 2026-08-15 — CHECKPOINT

### MILESTONE 1 — STABILIZATION CHECKPOINT — CLOSED

Migration history was stabilized.

Problems resolved:

1. PostgreSQL enum duplicate creation:
   - a14d46e05aea
   - 3bc06cbcc627
   - 8333a4651466
   Fix: `postgresql.ENUM(..., create_type=False)`

2. Missing qualifications migration:
   - added: `f1a9c3d2b7e4_create_qualifications_table.py`
   - positioned correctly in the chain:
     a14d46e05aea → f1a9c3d2b7e4 → 3bc06cbcc627

Migration chain (final):
7fa55a728c94
→ 91fa25437e98
→ a14d46e05aea
→ f1a9c3d2b7e4
→ 3bc06cbcc627
→ 8333a4651466
→ 913038ade754 (HEAD)


Fresh database validation:

- run successfully twice
- 7 migrations
- 11 tables
- qualifications present
- employee_qualifications present
- demand_tasks present
- shifts present
- employee_shifts present

The real development DB was NOT modified during migration history stabilization.

Milestone 1 is CLOSED and committed.

### MILESTONE 2 — DATABASE ENVIRONMENT CONSISTENCY — CLOSED

Commit: `0ec7aadb6cc96ac914c8b8b0ec6b3ec9bb57221b`

Commit status:
- branch: main
- working tree clean
- committed locally
- branch ahead of origin/main by 1 commit
- push to remote NOT done

Files modified (5):

1. `backend/app/db/session.py`
   - removed hardcoded `localhost:5432` fallback
   - `DATABASE_URL` is now required
   - explicit `RuntimeError` if missing

2. `backend/alembic.ini`
   - removed hardcoded `sqlalchemy.url`

3. `backend/alembic/env.py`
   - removed fallback onto `alembic.ini`
   - `DATABASE_URL` is now required
   - explicit `RuntimeError` if missing

4. `backend/.env.example`
   - configured for Hybrid development: `127.0.0.1:5433`
   - both scenarios documented:
     Hybrid: backend local + PostgreSQL in Docker
     Full Docker: backend + PostgreSQL both in Docker

5. `README.md`
   - Full Docker and Hybrid development documented explicitly
   - difference between host port 5433 and container port 5432 explained

Validations performed:
- `alembic current` → `913038ade754 (head)`
- `alembic heads` → `913038ade754 (head)`
- `alembic check` → "No new upgrade operations detected."
- `/health` → 200 OK, `{"status":"ok"}`

Not executed: `upgrade`, `downgrade`, `stamp`.

Observation for later (not part of Milestone 2, not addressed now):
- `uvicorn --reload` fails locally on this Windows/venv setup with
  `ModuleNotFoundError: No module named 'sqlalchemy'`.
- Without `--reload`, the app starts normally and `/health` responds 200 OK.

Milestone 2 is CLOSED and committed.

### CURRENT PROJECT STATE

**Backend:**
- FastAPI foundation: existent
- SQLAlchemy 2: existent
- Alembic: stabilized
- PostgreSQL/Docker: functional
- Organization, Department, Role, Qualification, Employee,
  EmployeeQualification, DemandTask, Shift, EmployeeShift: existent
- demand_calculator: existent
- workforce_coverage: existent
- authentication: not yet implemented
- automated testing: not yet implemented

**Frontend:**
- React + Vite + TypeScript foundation
- currently only the application scaffold
- functional UI not yet implemented

**Testing:**
- pytest not yet introduced
- no automated test baseline exists
- next planned milestone is Testing Baseline

### PROJECT PROGRESS (ESTIMATED)

This is progress toward the final CEDAR product, not "percentage of code
written." Stabilization milestones do not artificially inflate this
number; they increase the quality and safety of the foundation. This
percentage is an architectural-level ESTIMATE, not a precise
measurement — no official weighted methodology has been defined yet in
project documentation.

- Foundation/backend/data model: ~30%
- Core domain/API foundation: ~25%
- Infrastructure/database: ~15%
- Frontend/UI: ~0-5%
- Testing: 0%
- Authentication/security: 0%
- Production readiness: 0%

Overall estimated project completion: ~30-35%

### NEXT MILESTONE

**MILESTONE 3 — TESTING BASELINE**

Tests are not implemented yet. Before writing any test code, the
following must first be defined and approved by the architect:

1. which behaviors must be tested;
2. which testing levels will be used;
3. which endpoints are critical;
4. which services/domain rules are critical;
5. what must be verified for database/migrations;
6. what "Definition of Done" means for Testing Baseline.

### END OF DAY STATUS — 2026-08-15

- Milestone 1 — CLOSED
- Milestone 2 — CLOSED
- Migration history — STABLE
- Database environment configuration — STABLE
- Fresh DB migration rebuild — VALIDATED
- Development DB — PRESERVED
- Working tree — CLEAN
- Latest local commit: `0ec7aadb6cc96ac914c8b8b0ec6b3ec9bb57221b`
- Remote push — NOT DONE
- Next milestone — TESTING BASELINE